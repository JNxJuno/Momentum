import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jurij.momentum',
  appName: 'Momentum',
  webDir: 'dist', // WICHTIG: Hier muss 'dist' stehen!
};

export default config;